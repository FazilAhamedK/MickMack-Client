import { EnumParserPipe } from './enum-parser.pipe';

describe('EnumParserPipe', () => {
  it('create an instance', () => {
    const pipe = new EnumParserPipe();
    expect(pipe).toBeTruthy();
  });
});
