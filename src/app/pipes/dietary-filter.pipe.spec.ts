import { DietaryFilterPipe } from './dietary-filter.pipe';

describe('DietaryFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DietaryFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
